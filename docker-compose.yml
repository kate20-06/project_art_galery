version: '3.8'

services:
  grafana:
    image: grafana/grafana
    container_name: grafana-dashboard
    ports:
      - "3000:3000"
    volumes:
      - grafana-storage:/var/lib/grafana

volumes:
  grafana-storage:
